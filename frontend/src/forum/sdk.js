/* ══════════════════════════════════════════════════════════════════════════════
 * @bias/forum SDK — 前台扩展 API
 * ══════════════════════════════════════════════════════════════════════════════ */

export function registerDiscussionSidebarSection(app, section) {}
export function registerComposerComponent(app, composer) {}
export function registerDiscussionAction(app, action) {}
export function registerNotificationHandler(app, handler) {}
export function registerSearchFilter(app, filter) {}

export default { registerDiscussionSidebarSection, registerComposerComponent, registerDiscussionAction, registerNotificationHandler, registerSearchFilter }
